<view class="video">
  <view class="item" wx:for="{{topMVs}}" wx:key="id">
    <video-item-v1 
      item="{{item}}" 
      bind:tap="handleVideoItemTap" 
      data-item="{{item}}"
    />
  </view>
</view>
