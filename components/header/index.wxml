<view class="header">
  <view class="header-title">{{title}}</view>
  <view class="header-right" wx:if="{{showRight}}">
    <view class="slot"><slot/></view>
    <view class="default">
      <text>{{rightText}}</text>
      <image class="icon" src="/assets/images/icons/arrow-right.png"/>
    </view>
  </view>
</view>
