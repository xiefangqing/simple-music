<view>
  <header title="{{title}}" rightText="更多"/>
  <scroll-view scroll-x class="menu-list">
    <view class="menu-item" wx:for="{{songMenu}}" wx:key="id">
      <song-menu-item item="{{item}}"/>
    </view>
  </scroll-view>
</view>
